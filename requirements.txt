# 核心依赖 / Core dependencies
transformers
torchvision
tqdm
Pillow
numpy

# FFmpeg bindings (ensure FFmpeg functionality via Python)
ffmpeg-python>=0.2.0
imageio-ffmpeg>=0.4.9

pydub
scipy
httpx

# ASR 和字幕依赖 / ASR and Subtitle dependencies
faster-whisper>=0.10.0
transformers>=4.30.0
moviepy>=1.0.3
jieba>=0.42.1
langid>=1.1.6
huggingface-hub>=0.20.0

# 可选依赖 / Optional dependencies
# 人脸修复功能需要额外依赖，请参考 requirements_face_restore.txt
# Face restoration features require additional dependencies, see requirements_face_restore.txt

# 安装说明 / Installation instructions:
# 1. 基础功能：pip install -r requirements.txt
# 2. 人脸修复功能：pip install -r requirements_face_restore.txt
# 3. 完整安装：pip install -r requirements.txt -r requirements_face_restore.txt